#include <pch.h>
#include "AbilityIds.h"

using namespace entt::literals;

namespace AbilityIds
{
	const entt::id_type NullAbility = "null_ability"_hs;
	const entt::id_type ThrowAbility = "throw_ability"_hs;
	const entt::id_type SprintAbility = "sprint_ability"_hs;
}