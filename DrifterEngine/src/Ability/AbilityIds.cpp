#include <pch.h>
#include "AbilityIds.h"

using namespace entt::literals;

namespace AbilityIds
{
	const entt::hashed_string NullAbility = "null";
	const entt::hashed_string ThrowAbility = "throw";
	const entt::hashed_string SprintAbility = "sprint";
	const entt::hashed_string PowerAttackAbility = "power_attack";
}